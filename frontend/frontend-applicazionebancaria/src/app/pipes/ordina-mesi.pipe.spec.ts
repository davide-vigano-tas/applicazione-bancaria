import { OrdinaMesiPipe } from './ordina-mesi.pipe';

describe('OrdinaMesiPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdinaMesiPipe();
    expect(pipe).toBeTruthy();
  });
});
